apply plugin: 'java-library'
apply plugin: 'com.novoda.bintray-release'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"

//推送到Bintray配置
publish {
    repoName = 'zhuang'
    userOrg = "likeadog"
    groupId = "com.zhuang"
    artifactId = 'zcomponent'
    publishVersion = "1.0.0" //版本号
    desc = "modularization tool" //描述，选填
}

//gradlew clean build bintrayUpload -PbintrayUser=likeadog -PbintrayKey=a6f5d34620ca7c7768b7ee6a73f3be1091e6e8c1 -PdryRun=false